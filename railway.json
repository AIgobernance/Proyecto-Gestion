{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "a2dismod mpm_event mpm_worker && a2enmod mpm_prefork && apache2-foreground"
  }
}
